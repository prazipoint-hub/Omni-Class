# DEPRECATED — ecclesia-teacher

This folder has been deprecated and replaced by omniclass-teacher.

Why:
- The project was renamed from "ecclesia" to "omniclass" and the canonical teacher-backend now lives in the `omniclass-teacher/` directory.

What to use instead:
- See `/omniclass-teacher` for the current code, installation instructions, and development scripts.

If you still have CI/workflows or scripts referencing `ecclesia-teacher/`, please update them to point to `omniclass-teacher/`.

If you want to fully remove the deprecated folder from history, a maintainer can delete it in a follow-up commit or remove it when merging this branch. This README preserves a clear pointer in the repository history.

Committed on branch: feature/ecclesia-teacher-backend
